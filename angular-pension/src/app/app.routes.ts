import { Routes } from '@angular/router';
import { CalculatePensionComponent } from './calculate-pension/calculate-pension.component';

export const routes: Routes = [
    { path: '', component: CalculatePensionComponent }
];
 